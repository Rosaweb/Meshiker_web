# Même projet Supabase que l'application mobile (comptes et données partagés).
# Copier ce fichier en .env.local et renseigner les valeurs.
# La clé anon est publique par conception (protégée par le RLS) ;
# ne JAMAIS mettre ici la clé service_role / sb_secret_*.
NEXT_PUBLIC_SUPABASE_URL=https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
